# Configuration

`initContactFormConfig` configures contact categories, field definitions, messages, validation schemas, and service settings. Initialize it once in server startup code.

Generic color, spacing, focus, and control appearance are Goo/theme concerns. Forms CSS contains only form workflow layout, validation, status, upload, and thank-you treatment.
